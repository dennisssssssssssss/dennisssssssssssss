#include <bits/stdc++.h>

using namespace std;
ifstream f("date.in");

int n, x, y, p;
int main()
{
    f>>n;
    f>>x;
    n--;
    p = 1;
    while(n)
    {
        f>>y;
        if(x>y)
            p++;
        n--;
    }
    cout<<p;
    return 0;
}
