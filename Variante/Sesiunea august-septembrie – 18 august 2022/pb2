#include <bits/stdc++.h>

using namespace std;

int main()
{
    char s[50];
    cin.get(s,50);
    for(int i=0;i<strlen(s);i++)
        while(s[i]!='a' && s[i]!='e' && s[i]!='i' && s[i]!='o' &&s[i]!='u')
        s[i]--;
    cout<<s;

    return 0;
}
